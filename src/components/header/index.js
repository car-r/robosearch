import React from 'react'
import { HeaderContainer, HeaderTitle } from './HeaderElements'

const Header = () => {
    return (
        <HeaderContainer>
            <HeaderTitle>ROBOSEARCH</HeaderTitle>
        </HeaderContainer>
    )
}

export default Header
